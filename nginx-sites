server {
	listen 80 default_server;
	listen [::]:80 default_server;

	root /opt/unbound-stats;

	index index.html;

	server_name _;

	location / {
		auth_basic "Restricted Area";
		auth_basic_user_file /etc/nginx/.htpasswd;

		try_files $uri $uri/ =404;
	}

}
